interface Tutor {
    first_name: string;
    last_name: string;
    email: string;
    phone?: string; // Optional field
    hire_date: string;
    department?: string; // Optional field
    bio?: string; // Optional field
}
