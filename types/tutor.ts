interface Tutor {
    first_name: string;
    last_name: string;
    email: string;
    phone?: string; // Optional field
    hire_date: string;
    gender:string;
    department_name?: string; // Optional field
    bio?: string; // Optional field
}
